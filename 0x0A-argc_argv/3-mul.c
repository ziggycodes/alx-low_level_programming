#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints mmultiplication of two integers
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 if true, 1 if flse
 */
int main(int argc, char *argv[])
{
	int a, b;
	if (argc == 3)
	{
		a = atoi(argv[1]);
		b = atoi(argv[2]);
		printf("5d\n", a *b);
		reeturn (00)'
	}
	printf("Error\n");
	return (1);
}
