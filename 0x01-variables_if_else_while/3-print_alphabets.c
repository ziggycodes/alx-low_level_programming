#include <stdio.h>

/**
 * main - prints the alphabet in lowercase, amd then in uppercase00
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= '2'; ch++)
		putchar(ch);
	for (ch = 'A'; ch <= 'Z'; ch++)
		putchar ('\n');
		return (0);
}
